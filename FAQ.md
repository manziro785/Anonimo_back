# ❓ Часто задаваемые вопросы (FAQ)

## 🚀 Деплой и настройка

### В: Как получить Groq API ключ?

**О:** 
1. Зайдите на [console.groq.com](https://console.groq.com)
2. Зарегистрируйтесь или войдите
3. Перейдите в раздел "API Keys"
4. Нажмите "Create API Key"
5. Скопируйте ключ (начинается с `gsk_`)

### В: Какой DATABASE_URL использовать на Render?

**О:** Используйте **Internal Database URL**, не External! Найдите его в:
- Render Dashboard → PostgreSQL → Info → Internal Database URL
- Формат: `postgresql://user:password@dpg-xxxxx.region.render.com/dbname`

### В: Почему приложение "спит" на Render Free tier?

**О:** На бесплатном плане Render усыпляет приложение после 15 минут неактивности.
**Решения:**
- Перейти на платный план ($7/месяц)
- Использовать UptimeRobot для пинга каждые 10 минут
- Принять это как есть (первый запрос после сна будет медленным)

### В: Как изменить порт приложения?

**О:** Порт задается через переменную окружения `PORT`. На Render это автоматически 8080.
Для локального запуска измените в `.env`:
```env
PORT=3000
```

---

## 🔐 Безопасность

### В: Как создать безопасный JWT_SECRET?

**О:** Используйте случайную строку минимум 32 символа:

```bash
# В Linux/Mac
openssl rand -base64 32

# В Node.js
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

### В: Нужно ли хэшировать пароли вручную?

**О:** Нет! Sequelize hook автоматически хэширует пароли при создании пользователя через bcrypt.

### В: Как добавить нового ADMIN пользователя?

**О:** Через прямой SQL запрос к БД или измените роль существующего пользователя:

```sql
UPDATE users SET role = 'ADMIN' WHERE email = 'your@email.com';
```

---

## 💬 AI и сообщения

### В: Почему AI не отвечает?

**О:** Проверьте:
1. `GROQ_API_KEY` правильно указан в Environment Variables
2. У вас есть квота на Groq API (проверьте на console.groq.com)
3. Модель `llama-3.3-70b-versatile` доступна
4. Проверьте логи на Render для ошибок API

### В: Можно ли использовать другую AI модель?

**О:** Да! Измените `GROQ_MODEL` в .env:
- `llama-3.3-70b-versatile` (по умолчанию)
- `llama-3.1-70b-versatile`
- `mixtral-8x7b-32768`
- См. список на [console.groq.com/docs/models](https://console.groq.com/docs/models)

### В: Как увеличить лимит контекста для AI?

**О:** В `controllers/messageController.js` измените:
```javascript
// Было:
limit: 10

// Стало:
limit: 20 // или больше
```

---

## 🗄️ База данных

### В: Как посмотреть данные в PostgreSQL на Render?

**О:** 
1. Render Dashboard → PostgreSQL → Connect
2. Скопируйте PSQL Command
3. Выполните в терминале:
```bash
psql postgresql://user:pass@host/db
```

### В: Как сбросить базу данных?

**О:** **ВНИМАНИЕ! Удалит все данные!**

В psql:
```sql
DROP SCHEMA public CASCADE;
CREATE SCHEMA public;
```

Или в коде измените в `server.js`:
```javascript
await sequelize.sync({ force: true }); // Пересоздаст все таблицы
```

### В: Нужно ли запускать миграции?

**О:** Нет! Sequelize автоматически синхронизирует схему БД при запуске через:
```javascript
sequelize.sync({ alter: true })
```

---

## 🌐 WebSocket

### В: Как подключиться к WebSocket с фронтенда?

**О:**
```javascript
import io from 'socket.io-client';

const socket = io('https://your-app.onrender.com');

socket.emit('join-chat', chatId);
socket.on('new-message', (message) => {
  console.log(message);
});
```

### В: WebSocket не работает на Render

**О:** Убедитесь что:
1. Используете `https://` (не `http://`)
2. Socket.IO клиент версии 4.x
3. CORS правильно настроен в `ALLOWED_ORIGINS`

---

## 🔧 Разработка

### В: Как запустить проект локально?

**О:**
```bash
# 1. Установите зависимости
npm install

# 2. Настройте .env файл
cp .env.example .env
# Отредактируйте .env

# 3. Запустите PostgreSQL локально или используйте Docker
docker-compose up -d postgres

# 4. Запустите сервер
npm run dev
```

### В: Как использовать Docker?

**О:**
```bash
# Запустить всё (PostgreSQL + Backend)
docker-compose up

# Только база данных
docker-compose up postgres

# Остановить
docker-compose down
```

### В: Как добавить новый эндпоинт?

**О:**
1. Создайте контроллер в `/controllers`
2. Создайте роут в `/routes`
3. Подключите роут в `server.js`

Пример:
```javascript
// controllers/customController.js
exports.myEndpoint = async (req, res) => {
  res.json({ message: 'Hello!' });
};

// routes/custom.js
const router = require('express').Router();
const controller = require('../controllers/customController');
router.get('/my-endpoint', controller.myEndpoint);
module.exports = router;

// server.js
app.use('/api/v1/custom', require('./routes/custom'));
```

---

## 📊 Производительность

### В: Как увеличить скорость ответа AI?

**О:**
1. Уменьшите контекст (меньше сообщений в истории)
2. Используйте более быструю модель (например, `mixtral-8x7b`)
3. Увеличьте timeout в axios запросе

### В: База данных медленная

**О:**
1. Добавьте индексы (уже есть в SQL скрипте)
2. Используйте pagination для больших списков
3. Перейдите на платный план PostgreSQL на Render

### В: Как кэшировать ответы?

**О:** Добавьте Redis для кэширования:
```bash
npm install redis
```

---

## 🐛 Решение проблем

### В: "Failed to connect to database"

**О:** Проверьте:
- DATABASE_URL правильный и Internal (не External)
- PostgreSQL запущен
- Firewall не блокирует подключение
- Логин и пароль правильные

### В: "Cannot find module"

**О:**
```bash
# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install
```

### В: "Port already in use"

**О:**
```bash
# Найдите процесс на порту 8080
lsof -i :8080

# Убейте процесс
kill -9 <PID>

# Или измените порт в .env
PORT=3000
```

### В: Ошибка "CORS"

**О:** Добавьте URL фронтенда в `ALLOWED_ORIGINS`:
```env
ALLOWED_ORIGINS=https://your-frontend.vercel.app,http://localhost:3000
```

---

## 📱 Интеграция

### В: Как подключить фронтенд?

**О:**
```javascript
// В React/Next.js
const API_URL = 'https://your-app.onrender.com';

// Регистрация
const response = await fetch(`${API_URL}/api/v1/auth/register`, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ email, password, role: 'USER' })
});

const { token } = await response.json();
localStorage.setItem('token', token);

// Запросы с токеном
const profile = await fetch(`${API_URL}/api/v1/users/me`, {
  headers: { 'Authorization': `Bearer ${token}` }
});
```

### В: Как добавить файл-аплоад?

**О:** Добавьте multer:
```bash
npm install multer
```

---

## 💰 Стоимость

### В: Сколько стоит Render?

**О:**
- Free tier: $0 (ограничения: спящий режим, 750 часов/месяц)
- Starter: $7/месяц (постоянно активен)
- PostgreSQL Free: $0 (ограничения)
- PostgreSQL Starter: $7/месяц

### В: Сколько стоит Groq API?

**О:** На данный момент Groq API **бесплатный** с лимитами:
- 30 запросов в минуту
- 14,400 токенов в минуту

Для production рассмотрите платные альтернативы.

---

## 🎓 Обучение

### В: Где изучить Express.js?

**О:**
- [Официальная документация Express](https://expressjs.com)
- [MDN Web Docs](https://developer.mozilla.org)

### В: Где изучить PostgreSQL?

**О:**
- [PostgreSQL Tutorial](https://www.postgresqltutorial.com)
- [Sequelize Docs](https://sequelize.org/docs/v6/)

### В: Где изучить WebSocket?

**О:**
- [Socket.IO Documentation](https://socket.io/docs/v4/)
- [WebSocket MDN](https://developer.mozilla.org/en-US/docs/Web/API/WebSocket)

---

**Не нашли ответ? Создайте issue на GitHub!**
