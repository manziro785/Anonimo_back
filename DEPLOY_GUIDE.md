# 🚀 ПОШАГОВАЯ ИНСТРУКЦИЯ ПО ДЕПЛОЮ НА RENDER

## ✅ Что вам понадобится:

1. Аккаунт на [GitHub](https://github.com)
2. Аккаунт на [Render](https://render.com)
3. Groq API ключ ([console.groq.com](https://console.groq.com))

---

## 📝 ШАГ 1: Подготовка проекта

### 1.1 Создайте репозиторий на GitHub

```bash
# Инициализируйте git в папке проекта
cd anonimo-backend
git init

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Initial commit: Anonimo Backend"

# Создайте репозиторий на GitHub (через веб-интерфейс)
# Затем подключите его:
git remote add origin https://github.com/ваш-username/anonimo-backend.git

# Отправьте код
git branch -M main
git push -u origin main
```

---

## 🗄️ ШАГ 2: Создание PostgreSQL базы данных на Render

### 2.1 Зайдите на Render.com

1. Откройте [render.com](https://render.com)
2. Войдите или зарегистрируйтесь
3. Нажмите **Dashboard**

### 2.2 Создайте PostgreSQL сервис

1. Нажмите **New +** → **PostgreSQL**
2. Заполните форму:
   - **Name**: `anonimo-db` (или любое другое имя)
   - **Database**: `anonimo`
   - **User**: `anonimo_user` (автоматически)
   - **Region**: Выберите ближайший регион
   - **PostgreSQL Version**: 15 (или новее)
   - **Plan**: **Free** (или платный если нужно)

3. Нажмите **Create Database**

### 2.3 Скопируйте данные подключения

После создания БД:

1. Откройте созданную базу данных
2. Перейдите на вкладку **Info**
3. Найдите **Internal Database URL** (важно! не External)
4. Скопируйте URL (будет вида):
   ```
   postgresql://anonimo_user:ПАРОЛЬ@dpg-xxxxx.oregon-postgres.render.com/anonimo
   ```
5. **СОХРАНИТЕ ЭТУ СТРОКУ** - она понадобится позже!

---

## 🔑 ШАГ 3: Получение Groq API ключа

### 3.1 Зайдите на Groq Console

1. Откройте [console.groq.com](https://console.groq.com)
2. Войдите через Google или создайте аккаунт

### 3.2 Создайте API ключ

1. Перейдите в раздел **API Keys** в левом меню
2. Нажмите **Create API Key**
3. Введите название: `Anonimo Backend`
4. Нажмите **Submit**
5. **СКОПИРУЙТЕ КЛЮЧ** (будет вида `gsk_...`)
6. **СОХРАНИТЕ** - его больше не покажут!

---

## 🌐 ШАГ 4: Деплой бэкенда на Render

### 4.1 Создайте Web Service

1. На Render Dashboard нажмите **New +** → **Web Service**
2. Выберите **Build and deploy from a Git repository**
3. Нажмите **Next**

### 4.2 Подключите GitHub репозиторий

1. Если еще не подключен GitHub:
   - Нажмите **Connect GitHub**
   - Авторизуйтесь
   - Разрешите доступ Render

2. Найдите ваш репозиторий `anonimo-backend`
3. Нажмите **Connect**

### 4.3 Настройте параметры деплоя

Заполните форму:

**Basic Settings:**
- **Name**: `anonimo-backend` (или любое другое)
- **Region**: Тот же регион, что и БД (важно!)
- **Branch**: `main`
- **Root Directory**: оставьте пустым
- **Runtime**: `Node`

**Build & Deploy:**
- **Build Command**: 
  ```bash
  npm install
  ```
- **Start Command**:
  ```bash
  npm start
  ```

**Plan:**
- Выберите **Free** (или платный если нужно)

### 4.4 Добавьте переменные окружения

Прокрутите вниз до раздела **Environment Variables**

Нажмите **Add Environment Variable** и добавьте следующие:

| Key | Value |
|-----|-------|
| `NODE_ENV` | `production` |
| `PORT` | `8080` |
| `DATABASE_URL` | *Ваш Internal Database URL из шага 2.3* |
| `JWT_SECRET` | `your-super-secret-jwt-key-change-this-12345` (придумайте свой!) |
| `JWT_EXPIRATION` | `86400000` |
| `GROQ_API_KEY` | *Ваш Groq API ключ из шага 3.2* |
| `GROQ_MODEL` | `llama-3.3-70b-versatile` |
| `ALLOWED_ORIGINS` | `*` (или ваш фронтенд URL) |

**⚠️ ВАЖНО:**
- `DATABASE_URL` - вставьте **Internal** URL из PostgreSQL
- `JWT_SECRET` - придумайте свой случайный ключ (минимум 32 символа)
- `GROQ_API_KEY` - ключ из Groq Console
- `ALLOWED_ORIGINS` - для теста можно `*`, потом укажите URL фронтенда

### 4.5 Создайте сервис

1. Нажмите **Create Web Service**
2. Render начнет деплой (займет 2-5 минут)
3. Следите за логами в разделе **Logs**

---

## ✅ ШАГ 5: Проверка работы

### 5.1 Дождитесь успешного деплоя

В логах должно появиться:
```
✅ Database connection established successfully
✅ Database synchronized
🚀 Anonimo Backend Server Running
```

### 5.2 Скопируйте URL вашего API

В верхней части страницы будет URL вида:
```
https://anonimo-backend-xxxx.onrender.com
```

### 5.3 Проверьте health endpoint

Откройте в браузере:
```
https://anonimo-backend-xxxx.onrender.com/actuator/health
```

Должно вернуть:
```json
{"status": "UP"}
```

### 5.4 Проверьте главную страницу

Откройте:
```
https://anonimo-backend-xxxx.onrender.com/
```

Должно вернуть JSON с информацией об API.

---

## 🧪 ШАГ 6: Тестирование API

### 6.1 Тест регистрации

```bash
curl -X POST https://anonimo-backend-xxxx.onrender.com/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password123",
    "role": "USER"
  }'
```

Должно вернуть:
```json
{"token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."}
```

### 6.2 Тест получения профиля

Используйте полученный токен:

```bash
curl -X GET https://anonimo-backend-xxxx.onrender.com/api/v1/users/me \
  -H "Authorization: Bearer ВАШ_ТОКЕН"
```

Должно вернуть ваш профиль.

### 6.3 Тест создания чата и AI сообщения

```bash
# Создать чат
curl -X POST https://anonimo-backend-xxxx.onrender.com/api/v1/chat \
  -H "Authorization: Bearer ВАШ_ТОКЕН" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тестовый чат",
    "user": {"id": 1}
  }'

# Отправить AI сообщение
curl -X POST https://anonimo-backend-xxxx.onrender.com/api/v1/messages/ai \
  -H "Authorization: Bearer ВАШ_ТОКЕН" \
  -H "Content-Type: application/json" \
  -d '{
    "chatId": 1,
    "content": "Привет! Расскажи анекдот про программистов"
  }'
```

---

## 📊 ДОПОЛНИТЕЛЬНО: Инициализация БД через SQL

Если хотите вручную создать таблицы:

### 7.1 Подключитесь к БД

На странице PostgreSQL в Render:

1. Перейдите на вкладку **Connect**
2. Скопируйте **PSQL Command**
3. Выполните в терминале:
   ```bash
   psql postgresql://anonimo_user:PASSWORD@host/anonimo
   ```

### 7.2 Выполните SQL скрипт

Скопируйте содержимое файла `database-init.sql` и выполните в psql.

**⚠️ ПРИМЕЧАНИЕ:** Sequelize автоматически создаст таблицы при первом запуске, поэтому этот шаг **опционален**.

---

## 🎉 ГОТОВО!

Ваш бэкенд успешно задеплоен на Render!

**Ваш API доступен по адресу:**
```
https://anonimo-backend-xxxx.onrender.com
```

### Следующие шаги:

1. **Обновите фронтенд** - укажите URL вашего API
2. **Настройте CORS** - в `ALLOWED_ORIGINS` укажите URL фронтенда
3. **Тестируйте все эндпоинты** - убедитесь что всё работает
4. **Мониторьте логи** - через Render Dashboard → Logs

---

## 🔧 Решение проблем

### Проблема: "Failed to connect to database"

**Решение:**
1. Проверьте `DATABASE_URL` в Environment Variables
2. Убедитесь что используете **Internal** URL, не External
3. Проверьте что БД и Web Service в одном регионе

### Проблема: "Groq API error"

**Решение:**
1. Проверьте `GROQ_API_KEY` в Environment Variables
2. Убедитесь что ключ активен на console.groq.com
3. Проверьте квоту API на Groq

### Проблема: "Application failed to respond"

**Решение:**
1. Проверьте логи в Render Dashboard
2. Убедитесь что `PORT=8080` в Environment Variables
3. Проверьте что Start Command: `npm start`

### Проблема: Free tier спит после 15 минут неактивности

**Решение:**
- Используйте платный план
- Или настройте ping каждые 10 минут (например, через UptimeRobot)

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Render Dashboard
2. Убедитесь что все Environment Variables правильно заполнены
3. Проверьте что используете Internal Database URL

**Успешного деплоя! 🚀**
